
version: 2

models:
  - name: my_first_test_model
    description: "A starter dbt model with import"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

